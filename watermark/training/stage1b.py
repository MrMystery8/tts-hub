"""
LEGACY SHIM: Stage 1B (bit-payload curriculum training).

The current watermarking pipeline uses multiclass attribution and does not use Stage 1B.
This module is kept only so older experiments/imports can still resolve.
"""

from watermark.legacy.stage1b import *  # noqa: F403

